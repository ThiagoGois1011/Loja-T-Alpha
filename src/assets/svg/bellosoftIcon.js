import image from "../../assets/image/logo.png";

function BelloSoftIcon({className}) {
    return(
        <img className={className} src={image} alt="logo da empresa"/>
    );
}

export default BelloSoftIcon;